// Test template with various MTL features
category = "369"
catid = 1b
image = "<green>333 6666 <red>666 <black>9999"
name = "Test template with multiple features"

description = "This template demonstrates various MTL features"

// A simple variable assignment
suit1 = 'b'  // bamboo
suit2 = 'c'  // characters
suit3 = 'd'  // dots

// A function call with arguments
pung(3, suit1)

// A more complex expression
if (suit1 == 'b' && suit2 == 'c') {
    // Nested function calls
    kong(6, suit1)
    pung(6, suit2)
    kong(9, suit3)
}

// A foreach loop
foreach (s in [suit1, suit2, suit3]) {
    // Function call with variable
    chow([1, 2, 3], s)
}

// A more complex expression with built-in functions
permutations([suit1, suit2, suit3], 2)
